//#include FreeRTOS files

//add extra state for hold state -> wait for xxStart = 1 then go to S0
//add extra state for hold state -> wait for xxStart = 1 then go to S0
//add extra state for hold state -> wait for xxStart = 1 then go to S0
//add extra state for hold state -> wait for xxStart = 1 then go to S0
//add extra state for hold state -> wait for xxStart = 1 then go to S0


typedef enum{
    S0,
    S1,
    S2,
    S3
} State;

State state = S0;

void mainFSM(){
    switch(state){
    }
}
